{
  "name": "comms-joes9987",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev --filter=@euda/web",
    "build": "turbo run build --filter=@euda/web",
    "lint": "turbo run lint --filter=@euda/web",
    "test": "turbo run test --filter=@euda/web",
    "typecheck": "turbo run typecheck --filter=@euda/web",
    "seed:reviewer": "pnpm --filter @euda/web seed:reviewer"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
